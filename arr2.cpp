#include <bits/stdc++.h>
using namespace std;

int N;
cin>>N;
 
int main() 
{
   int arr[i];
   cin>>arr[i];

   cout<<"Maximum Value = "<<(max_element(arr, arr+N))<<endl;
   cout<<"Minimum Value = "<<(min_element(arr, arr+N));
   return 0;
}